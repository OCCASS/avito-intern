# Проект на стажировку Avito (Backend)

## Мои доработки и решения проблем

- Мне нужно было получать команду пользователя и ее участников, чтобы назначить проверяющего pull request-а, поэтому я решил поставить условие, что **пользователь может быть только в одной команде**.

- При переназначении пользователя в pull request возникла такая проблема, может быть такое, что участников команды на переназначение нет, такое может возникнуть, когда количество участников команды три и меньше (двое или один уже проверяют, а один автор), поэтому я принял решение, что **переназначить проверяющего можно только если количество участников дает такую возможность**.

## Нагрузочные тесты

Результаты нагрузочного тесторования запроса `POST /team/add`

```
99th percentile: 4.804792ms
Max latency: 22.638417ms
Success rate: 100.00%
Total requests: 3000
Throughput: 100.03 requests/sec
```

## Запуск проекта

### Для "продакшена"

```bash
cp ./config/config.yml.example ./config/docker.yml
```

Далее нужно изменить значения на нужные в `./config/docker.yml`.

```bash
cp .env.docker.example .env.docker
```

После чего нужно изменить значения на нужные в `.env.docker`

```bash
docker-compose --env-file .env.docker up
```

**🔥 Готово! Проект запущен!**

### Для разработки

```bash
cp ./config/config.yml.example ./config/local.yml
```

Далее нужно изменить значения на нужные в `./config/local.yml`.

```bash
make run
```

**☕️ Готово! Проект запущен можно работать!**
