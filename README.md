# Проект на стажировку Avito (Backend)

## Нагрузочные тесты

Результаты нагрузочного тесторования запроса `POST /team/add`

```
99th percentile: 4.804792ms
Max latency: 22.638417ms
Success rate: 100.00%
Total requests: 3000
Throughput: 100.03 requests/sec
```

## Запуск проекта

### Для "продакшена"

Копируем файлы для конфигурации и запускаем проект

> [!NOTE]
> Все названия в `example` кофигах подобраны друг к другу так, чтобы все запустилось сразу из коробки

```bash
cp ./config/docker.yml.example ./config/docker.yml
cp .env.docker.example .env.docker
docker-compose --env-file .env.docker up
```

**🔥 Готово! Проект запущен!**

### Для разработки

```bash
cp ./config/config.yml.example ./config/local.yml
```

Далее нужно изменить значения на нужные в `./config/local.yml`.

```bash
make run
```

**☕️ Готово! Проект запущен можно работать!**

## Запуск тестов

```bash
make test
```

## Запуск нагрузечного тестирования

```bash
make load_test
```
